/**
 * Removed in session 8b — unused. `git rm` this file.
 *
 * Expo-template boilerplate. Word Hug follows the OS theme and offers no
 * in-app switch; Settings has no row for it, by design.
 */
export {};
