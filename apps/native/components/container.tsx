/**
 * Removed in session 8b — unused. `git rm` this file.
 *
 * Expo-template boilerplate. It wrapped children in `bg-background`, a heroui
 * default token that no Word Hug screen uses; every screen builds its own
 * ground from `PuzzleGround` and `bg-wh-ground`. It was the only importer of
 * `tailwind-merge` and `tailwind-variants` in the native app.
 */
export {};
